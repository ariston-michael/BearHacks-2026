// Translates a recognized gesture intent into a concrete system action via nut-js.
// Handles cursor movement, mouse clicks, scrolling, and keyboard shortcut injection.

export function dispatchAction(_intent: string): void {}
