// Combines the classified gesture from each hand into a single intent (e.g. "cmd-tab", "copy").
// Applies precedence rules so combined shortcuts take priority over standalone gestures.

export function combineGestures(): null {
  return null
}
