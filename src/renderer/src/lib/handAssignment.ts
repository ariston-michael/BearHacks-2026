// Determines which detected hand is the left (modifier) and which is the right (cursor).
// Uses handedness confidence scores from MediaPipe and applies sticky assignment to avoid flipping.

export function assignHands(): void {}
