// Records and persists the user's hand range-of-motion samples collected during the calibration flow.
// Computes min/max bounds and stores them in settingsStore for use by cursorMapper.

export function saveCalibration(): void {
  return undefined
}
