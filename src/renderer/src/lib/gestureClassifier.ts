// Classifies a single hand's landmark array into a named gesture (e.g. Pinch, Palm, C-shape, V-sign).
// Each classifier function receives 21 normalized landmarks and returns a GestureName or null.

export function classifyGesture(): null {
  return null
}
