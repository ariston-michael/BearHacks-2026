// Initializes and manages the MediaPipe Hands model lifecycle.
// Feeds camera frames into the model and emits raw landmark results for downstream processing.

export function initHandDetection(): void {}
