// Collapsible reference panel listing every supported gesture, its hand assignment,
// and the action it triggers. Pulls data from the gesture binding config in settingsStore.

export default function GestureLegend(): React.JSX.Element {
  return <div>GestureLegend</div>
}
