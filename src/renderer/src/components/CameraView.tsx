// Renders the live webcam feed inside a canvas element and overlays
// MediaPipe hand landmarks drawn by the drawing_utils helpers.

export default function CameraView(): React.JSX.Element {
  return <div>CameraView</div>
}
