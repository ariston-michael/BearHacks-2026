// Small overlay showing the mapped cursor position relative to the user's hand position.
// Useful during calibration and for visual feedback that hand tracking is active.

export default function CursorPreview(): React.JSX.Element {
  return <div>CursorPreview</div>
}
