// Displays the currently detected gesture for each hand (e.g. "Pinch", "Palm", "C-shape").
// Updates in real time from gestureStore and highlights active modifier combinations.

export default function GestureIndicator(): React.JSX.Element {
  return <div>GestureIndicator</div>
}
