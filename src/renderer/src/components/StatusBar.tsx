// Persistent bottom bar showing connection status, FPS, hand-detection confidence,
// and whether AirControl is currently active or paused.

export default function StatusBar(): React.JSX.Element {
  return <div>StatusBar</div>
}
